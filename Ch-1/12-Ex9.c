#include <stdio.h>

main(){
    long reader;
    long str;
    while((reader = getchar())!=EOF){
        if (reader == ' ')
            putchar(reader);

        else { 
            putchar(reader);
        }
        
    }
}